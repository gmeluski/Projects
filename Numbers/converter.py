conversionPrompts = {
    't': 'Enter a temperature in Celsius',
    'c': 'Enter an amount in dollars',
    'm': 'Enter a weight in pounds'
}

def convertTemperature(temperature):
    print

def f(x):
    
    originalNumber = raw_input(conversionPrompts[conversionDirection])
    return {
        'a': convertTemperature(),
        'b': 2,
    }[x]

conversionDirection = raw_input('Convert by temperature, currency or mass (t / c / m): ')
f(conversionPoint)
